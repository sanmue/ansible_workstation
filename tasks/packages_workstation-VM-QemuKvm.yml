- name: install VM QEMU/KVM (all distros with DesktopEnv - virt-manager)
  tags: VM,workstation
  package:
    state: latest
    name:
      - virt-manager
#  when: ansible_distribution == "Ubuntu" and
#        ansible_virtualization_role != "guest"
  when: ansible_virtualization_role != "guest"
