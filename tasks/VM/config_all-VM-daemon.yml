- name: VM - start and enable libvirtd (all)
  tags: VM
  ansible.builtin.service:
    name: libvirtd.service
    state: started
    enabled: true
  when:
    - ansible_facts['virtualization_role'] != "guest"
