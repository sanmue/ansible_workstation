
- name: start libvirtd (all)
  tags: VM
  service:
    name: libvirtd.service
    state: started
  when: ansible_virtualization_role != "guest"

- name: enable libvirtd (all)
  tags: VM
  service:
    name: libvirtd.service
    enabled: yes
  when: ansible_virtualization_role != "guest" 
